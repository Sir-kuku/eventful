{
  "name": "eventful-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "cd eventful-web && npm install && npm run build",
    "start": "node dist/server.js",
    "test": "jest"
  }
}
